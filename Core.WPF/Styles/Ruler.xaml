<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Imagin.Core.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{local:StyleKey Key=FrameworkElement}"/>
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="{x:Type local:Ruler}" TargetType="{x:Type local:Ruler}" BasedOn="{StaticResource {x:Type FrameworkElement}}">
        <Setter Property="AutoSize" Value="True" />
        <Setter Property="Background" Value="{local:ThemeKey RulerBackground}" />
        <Setter Property="BorderBrush" Value="{local:ThemeKey RulerBorder}" />
        <Setter Property="ChipColor" Value="#22000000" />
        <Setter Property="Foreground" Value="{local:ThemeKey RulerForeground}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Resolution" Value="72" />
        <Setter Property="SmallStep" Value="10" />
        <Setter Property="Step" Value="50" />
        <Setter Property="TickPlacement" Value="Top" />
        <Setter Property="TickStroke" Value="{local:ThemeKey RulerTickStroke}" />
        <Setter Property="Unit" Value="Pixel" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>